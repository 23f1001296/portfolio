name: Test Action with Email Step

on:
  workflow_dispatch: # allows manual trigger
  push:             # also runs when you push

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: 23f1001296@ds.study.iitm.ac.in
        run: echo "Hello, world!"
